services:
  bot:
    build:
      context: ./bot
    container_name: ai-agent-bot
    env_file:
      - ./bot/.env
    restart: unless-stopped

  backend:
    build:
      context: ./backend
    container_name: ai-agent-backend
    ports:
      - "8000:8000"
    restart: unless-stopped